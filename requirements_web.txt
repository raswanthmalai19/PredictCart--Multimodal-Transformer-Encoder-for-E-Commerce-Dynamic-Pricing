# E-Commerce Price Predictor - Requirements
# Python 3.8+ required
# Optimized for Mac M1/M2 (Apple Silicon)

# Web Framework
Flask>=2.3.2
Werkzeug>=2.3.6

# Deep Learning (M1-optimized)
torch>=2.0.0
torchvision>=0.15.0

# NLP & Transformers (updated for compatibility)
transformers>=4.35.0
tokenizers>=0.15.0
sentencepiece>=0.1.99
safetensors>=0.4.0

# Data Processing (numpy 1.x for compatibility)
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Utilities
tqdm>=4.65.0
pillow>=10.0.0

# Optional (for production deployment)
gunicorn>=20.1.0  # For production WSGI server
python-dotenv>=1.0.0  # For environment variables
